<%-
model = type.map[:model].type
gen_type = type.map[:gen_type].type
-%>

func <%= model.name %>SliceToResultSlice(s *[]*<%= model.qualified_name %>) *[]*<%= gen_type.qualified_name(dependencies) %> {
  if s == nil {
    return nil
  }
  r := []*<%= gen_type.qualified_name(dependencies) %>{}
  for _, m := range *s {
    r = append(r, <%= model.name %>ToResult(m))
  }
  return &r
}
